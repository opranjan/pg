import 'package:flutter/material.dart';

class MoneyCollection extends StatelessWidget {
  const MoneyCollection({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("No Collections"),
    );
  }
}